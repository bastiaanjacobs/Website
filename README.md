## 🌐 bastiaanjacobs website

A minimal Vite + React setup with SCSS support and GitHub Pages deployment.

## 🚀 Getting Started

Install dependencies:

```bash
npm install
```

Start the development server:

```bash
npm run dev
```

The app will be available at the local Vite dev URL.

## 🛠 Available Scripts

* **`npm run dev`** — Start the Vite development server
* **`npm run build`** — Build the project for production into `dist/`
* **`npm run preview`** — Preview the production build locally
* **`npm run deploy`** — Deploy the `dist/` folder to GitHub Pages

## 🧱 Tech Stack

* [Vite](https://vitejs.dev/) — Fast frontend build tool
* React (via `@vitejs/plugin-react`)
* SCSS (Sass)
* GitHub Pages for deployment

## 📂 Build Output

Production files are generated in the `dist/` directory after running:

```bash
npm run build
```

## 🌐 Deployment

To deploy to GitHub Pages:

```bash
npm run build
npm run deploy
```



---

Minimal setup, fast builds, and simple deployment.
